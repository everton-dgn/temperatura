import React from "react";
import {CompCity} from './styles';

const City = () => {

    return (
        <CompCity>
            <div>
                <button>AA</button>
            </div>
            <div>
                Cidade
            </div>
            <div>
                <button>BB</button>
            </div>
        </CompCity>
    );
};

export default City;