import React from "react";
import {CompCity} from './styles';

const City = ({city}) => {

    return (
        <CompCity>
            <div>
                {/*<h1>{city}</h1>*/}
            </div>
        </CompCity>
    );
};

export default City;