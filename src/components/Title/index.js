import React from "react";
import {CompTitle} from './styles';

const Title = () => {

    return (
        <CompTitle>
            <div>
                <h1>TEMPERATURA</h1>
            </div>
        </CompTitle>
    );
};

export default Title;